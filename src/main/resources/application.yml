spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agerdon?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: ${DB_PASSWORD:Tcclee8747!!}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  security:
    jwt:
      token:
        secret-key: yourSecretKeyMustBeVeryLongAndSecureBase64EncodedStringExample1234567890
        expire-length: 3600000